<UserControl x:Class="RealEstateModule.Views.Dialogs.ImportRealEstateDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RealEstateModule.Views.Dialogs"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"></Grid>
        <TextBlock Grid.Row="1">请选择导入的项目</TextBlock>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBox Text="{Binding FilePath}" MinWidth="100"/>
            <Button Command="{Binding ChooseFileCommand}">选择</Button>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button Command="{Binding ImportRealEstateCommand}">确认</Button>
            <Button Command="{Binding CancelCommand}">取消</Button>
        </StackPanel>
    </Grid>
</UserControl>
